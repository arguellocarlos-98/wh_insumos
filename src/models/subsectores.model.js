import * as Sentry from "@sentry/node";
import { queryListarSubsector } from "../queries/subsectores.queries.js";
import { listarProcedure } from "../db/operations.db.js";

export const modelListarSubsector = async (params) => {
    const paramsQuery = [
        
    ];  
};