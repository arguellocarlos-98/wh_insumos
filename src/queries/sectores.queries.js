// ABM DE SECTORES
// LISTAR SECTORES
export const queryListarSector = "CALL `sp_listarSector`(?,?);";

// INSERTAR SECTORES
export const queryInsertarSector = "CALL `sp_insertarSector`(?,?);";

// EDITAR SECTORES
export const queryEditarSector = "CALL `sp_editarSector`(?,?,?);";

// MANTENER SECTORES
export const queryMantenerSector = "CALL `sp_mantenerSector`(?,?,?);";